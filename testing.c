#include <stdio.h>

void main() {
    // Declare the variables.
    int alas;
    int tinggi;
    float luas;

    // assign a value into each variable
    alas = 7;
    tinggi = 10;

    luas = alas * tinggi;

    // print the variable into the screen
    printf("Alas = %d", alas);
    printf("\nTinggi = %d", tinggi);

}

